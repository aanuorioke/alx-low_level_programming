#include "main.h"

/**
 * _abs - function
 * Description: determines abs value of an integer
 * @c: number whose abs value is to be determined
 * Return: int
 */
int _abs(int n)
{
	return (abs(n));
}
