#include "main.h"

/**
 * _abs - function
 * Description: determines abs value of an integer
 * @n: number whose abs value is to be determined
 * Return: int
 */
int _abs(int n)
{
	return (abs(n));
}
